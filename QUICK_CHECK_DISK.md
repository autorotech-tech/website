# Быстрая проверка: расширен ли диск?

## 🔍 Выполните в rescue mode:

```bash
fdisk -l /dev/sda | grep "Disk /dev/sda"
```

## 📊 Результаты:

### Если показывает 75G:
```
Disk /dev/sda: 75 GiB, 80530636800 bytes
```
**✅ Вы правы!** Провайдер **НЕ расширил** диск. Нужно обратиться в поддержку.

### Если показывает больше 75G:
```
Disk /dev/sda: 100 GiB, 107374182400 bytes
```
**Диск расширен!** Нужно создать раздел на свободном месте.

## 📝 Детальная проверка:

```bash
# Посмотреть все разделы и свободное место
fdisk -l /dev/sda

# Или через parted
parted /dev/sda print free
```

## 🎯 Что делать:

### Если диск НЕ расширен (75G):
1. Обратиться в поддержку Contabo
2. Указать что докупили место, но оно не отображается
3. Попросить расширить диск

### Если диск расширен (>75G):
Следуйте инструкциям из `SAFE_PARTITION_MOUNT.md` для создания раздела.

