# Безопасное монтирование нового раздела

## ⚠️ ВАЖНО: Не сломать существующую систему!

Вы работаете на **рабочем сервере** с запущенными сервисами. Нужно быть осторожным.

## 🔍 Шаг 1: Определить новый раздел

### В rescue mode (chroot):

```bash
# 1. Посмотреть все разделы
lsblk -f
# или
fdisk -l

# 2. Посмотреть какие разделы уже примонтированы
df -h
mount | grep -v loop
```

### Что искать:
- **Новый раздел** будет **БЕЗ** точки монтирования в `df -h`
- Обычно это самый большой неиспользуемый раздел
- Может быть `sda4`, `sda5`, или другой номер

## 🔧 Шаг 2: Определить тип файловой системы

```bash
# Проверить тип файловой системы нового раздела
blkid /dev/sdaX  # где X - номер нового раздела

# Или
file -s /dev/sdaX
```

## 📋 Шаг 3: Безопасное монтирование

### A. Если раздел уже отформатирован (ext4, xfs и т.д.):

```bash
# 1. Создать точку монтирования (например, /mnt/storage или /storage)
mkdir -p /storage

# 2. Примонтировать временно (для проверки)
mount /dev/sdaX /storage

# 3. Проверить что все работает
df -h
ls -la /storage

# 4. Если все ОК, добавить в /etc/fstab для постоянного монтирования
```

### B. Если раздел НЕ отформатирован:

```bash
# 1. Отформатировать (ОСТОРОЖНО! Убедитесь что это правильный раздел!)
mkfs.ext4 /dev/sdaX
# или
mkfs.xfs /dev/sdaX

# 2. Создать точку монтирования
mkdir -p /storage

# 3. Примонтировать
mount /dev/sdaX /storage
```

## 🔐 Шаг 4: Добавить в /etc/fstab для автозагрузки

```bash
# 1. Получить UUID раздела
blkid /dev/sdaX
# Записать UUID (например: UUID=12345678-1234-1234-1234-123456789abc)

# 2. Отредактировать /etc/fstab
nano /etc/fstab
# или
vi /etc/fstab

# 3. Добавить строку (в конец файла):
UUID=12345678-1234-1234-1234-123456789abc  /storage  ext4  defaults  0  2

# Формат:
# UUID=<uuid>  <mount_point>  <filesystem>  <options>  <dump>  <pass>
# defaults - стандартные опции монтирования
# 0 - не делать backup через dump
# 2 - проверять файловую систему при загрузке (после root)
```

### Примеры для разных файловых систем:

```bash
# ext4
UUID=xxx  /storage  ext4  defaults  0  2

# xfs
UUID=xxx  /storage  xfs  defaults  0  0

# Если хотите только для чтения (не рекомендуется):
UUID=xxx  /storage  ext4  ro,defaults  0  2
```

## ✅ Шаг 5: Проверить перед перезагрузкой

```bash
# 1. Проверить синтаксис /etc/fstab
mount -a
# Если ошибок нет - все ОК

# 2. Проверить что раздел примонтирован
df -h | grep storage

# 3. Проверить права доступа
ls -la /storage
chown vladx:vladx /storage  # если нужно
chmod 755 /storage
```

## 🎯 Полная последовательность команд:

```bash
# === В CHROOT (после mount /dev/sda3 /mnt и chroot) ===

# 1. Найти новый раздел
lsblk -f
df -h

# 2. Определить новый раздел (например sda4)
blkid /dev/sda4

# 3. Если не отформатирован - отформатировать
# mkfs.ext4 /dev/sda4  # ТОЛЬКО если раздел пустой!

# 4. Создать точку монтирования
mkdir -p /storage

# 5. Примонтировать
mount /dev/sda4 /storage

# 6. Получить UUID
blkid /dev/sda4
# Записать UUID

# 7. Добавить в /etc/fstab
echo "UUID=<ваш-uuid>  /storage  ext4  defaults  0  2" >> /etc/fstab

# 8. Проверить
mount -a
df -h | grep storage

# 9. Установить права
chown vladx:vladx /storage
chmod 755 /storage
```

## ⚠️ Меры предосторожности:

1. **НЕ форматируйте** разделы которые уже используются!
2. **Проверьте дважды** номер раздела перед форматированием
3. **Сделайте backup** /etc/fstab перед редактированием:
   ```bash
   cp /etc/fstab /etc/fstab.backup
   ```
4. **Проверьте** `mount -a` перед перезагрузкой
5. **НЕ удаляйте** существующие строки в /etc/fstab

## 🔍 Как определить какой раздел новый:

```bash
# Посмотреть размеры разделов
lsblk -o NAME,SIZE,FSTYPE,MOUNTPOINT

# Новый раздел будет:
# - БЕЗ MOUNTPOINT (не примонтирован)
# - Самый большой неиспользуемый
# - Может быть без FSTYPE (если не отформатирован)
```

## 📝 Пример вывода lsblk:

```
NAME   SIZE FSTYPE MOUNTPOINT
sda    75G
├─sda1  1M
├─sda2  2G  ext4   /boot
├─sda3 73G  ext4   /
└─sda4 50G         <-- НОВЫЙ РАЗДЕЛ (без MOUNTPOINT)
```

В этом случае `sda4` - новый раздел для монтирования.

