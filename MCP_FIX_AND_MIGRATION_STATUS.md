# Статус: Исправление MCP и начало миграции

## ✅ Шаг 1: Исправление MCP-серверов (ЗАВЕРШЕНО)

### Что было исправлено:
1. ✅ Удален пустой `env` из `brave-search` (вызывал ошибки подключения)
2. ✅ Исправлен путь SSH ключа в `working-mcp-docker` (заменен `~/.ssh/` на абсолютный путь)

### Что нужно сделать:
1. **Перезапустить Cursor IDE** для применения изменений MCP-конфигурации
2. После перезапуска проверить, что серверы подключаются:
   - ✅ `nextjs-docs` (уже работает)
   - ✅ `tailwindcss` (должен заработать)
   - ✅ `filesystem` (должен заработать)
   - ✅ `puppeteer` (должен заработать)
   - ⚠️ `brave-search` (требует API ключ - добавить вручную при необходимости)

### Файлы:
- Backup создан: `~/.cursor/mcp.json.backup.20260106_003304`
- Скрипт исправления: `fix_mcp_config.sh`

## 🚀 Шаг 2: Начало миграции стека (ГОТОВО К НАЧАЛУ)

### Текущее состояние:
- Next.js: 14.2.14
- React: 18.3.1
- Tailwind CSS: 3.4.3

### План миграции:
См. `MIGRATION_PLAN.md` для детального плана

### Первые шаги:
1. ⚠️ **Убрать debug логи** из кода (остались от предыдущих дебагов)
2. Обновить Next.js до 15.x
3. Обновить React до 19.x
4. Добавить Shadcn UI

## 📋 Следующие действия:

1. **Перезапустить Cursor IDE** (для применения MCP-исправлений)
2. Подтвердить, что MCP-серверы работают
3. Начать миграцию (после подтверждения работы MCP)



